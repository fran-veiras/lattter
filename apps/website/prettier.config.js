const options = {
    arrowParens: 'avoid',
    singleQuote: true,
    bracketSpacing: true,
    tabWidth: 4,
    semi: false,
    PluginArray: ['prettier-plugin-tailwindcss'],
}

module.exports = options
